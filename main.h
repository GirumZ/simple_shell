#ifndef MAIN_H
#define MANE_H

#include <stdio.h>
#include <stdlib.h>

int _strlen(char *s);
int _strcmp(char *s1, char *s2);
char *str_concat(char *s1, char *s2);
char *_strdup(char *str);

#endif /* MAIN_H */
